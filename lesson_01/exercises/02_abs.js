// Given a number, return the absolute number, or only the positive version
// A negative number can be turned into a positive number by multiplying by -1

var num = -20;
var abs;

// your code goes here;
  abs = num;
// your code goes here;
  abs = num * -1;

console.log(abs);
